apiVersion: v1
kind: Service
metadata:
  name: {{ include "humorvibes.fullname" . }}
  labels:
    {{- include "humorvibes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "humorvibes.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
